@model Post
@using TheDailyPost.Models
@using Microsoft.AspNetCore.Identity
@using TheDailyPost.Areas.Identity.Data

@inject SignInManager<AuthUser> SignInManager
@inject UserManager<AuthUser> UserManager
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddComment">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input name="id" type="hidden" asp-for="Id" />
            @*<div class="form-group">
            <label asp-for="Commented_Post_id" class="control-label"></label>
            <input asp-for="Commented_Post_id" class="form-control"  />
            <span asp-validation-for="Commented_Post_id" class="text-danger"></span>
        </div>*@
            <div class="form-group">
                <input hidden name="commentorname" class="form-control" value="@UserManager.GetUserName(User).Substring(0,UserManager.GetUserName(User).IndexOf("@"))" />
                <textarea name="comment" required class="form-control"></textarea>
            </div>
            <div class="form-group">
                <input type="submit" value="Comment" class="btn" style="background: #2772c2;color:white;" />
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
